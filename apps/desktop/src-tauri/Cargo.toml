[package]
name = "edytlab-desktop"
version = "0.1.0"
description = "edytlab — conversational audio editor (Tauri shell)"
edition = "2021"
publish = false

[lib]
name = "edytlab_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
